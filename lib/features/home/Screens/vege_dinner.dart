import 'package:flutter/material.dart';

class VegeDinner extends StatelessWidget {
  const VegeDinner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}